/**
 * Optional, editable local aliases for names printed in another language.
 * They are deck-preparation data, not rules and never trigger a Scryfall request
 * while a physical game is in progress.
 */
export const localizedCardAliases: Record<string, string[]> = {}
